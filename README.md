# Mini PGW

## 📋 О проекте

Mini PGW (Packet Gateway) - это упрощенная модель сетевого компонента PGW, разработанная в рамках выпускной работы по C++. Проект реализует обработку UDP-запросов, управление сессиями абонентов по IMSI, ведение CDR-журнала и предоставление HTTP API.

## Содержание
1. Основные возможности
2. Архитектура  
2.1 pgw-server  
2.2 pgw_client
3. Структура проекта
4. Требования
5. Быстрый старт
6. Конфигурация  
6.1 Конфигурация сервера  
6.2 Конфигурация клиента
7. HTTP API
8. Форматы данных
9. Логирование
10. Используемые библиотеки
11. Особенности реализации

## 🎯 Основные возможности

- Обработка UDP-пакетов с IMSI в BCD-кодировке
- Управление сессиями абонентов с таймаутом
- CDR-журналирование
- HTTP API для мониторинга и управления
- Черный список IMSI с автоматическим отклонением запросов
- Graceful shutdown с контролируемым завершением сессий
- Конфигурация через JSON файлы
- Многоуровневое логирование (DEBUG, INFO, WARN, ERROR, CRITICAL)

## 🏗️ Архитектура
### pgw_server - Серверная часть
- UDP сервер для приема запросов создания сессий
- HTTP сервер для API управления
- Менеджер сессий с таймаутом
- CDR-логгер
- Менеджер черного списка

### pgw_client - Клиентская часть
- UDP клиент для отправки тестовых запросов
- Обработка ответов сервера
- Логирование операций

## 📁 Структура проекта
```text
PGW_PROTEI/
├── client/                         # Клиентская часть
│   ├── client_main.cpp             # Точка входа клиента
│   ├── client.cpp                  # Основная логика клиента
│   ├── client.h                    # Заголовочный файл клиента
│   ├── CMakeLists.txt              # Конфигурация сборки клиента
│   ├── client_config.json          # Конфигурация клиента
│   └── libs/                       # Общие библиотеки
│       ├── BCD/
│       │   ├── bcd.cpp             # BCD-кодирование/декодирование
│       │   └── bcd.h
│       ├── ConfigParser/
│       │   ├── config_parser.cpp   # Парсер JSON конфигурации
│       │   └── config_parser.h
│       ├── FileHandler/
│       │   ├── FileHandler.cpp     # Обработка файловых операций
│       │   └── FileHandler.h
│       ├── functions.cpp           # Вспомогательные функции
│       ├── functions.h
│       ├── Logger/
│       │   ├── logger.cpp          # Система логирования
│       │   └── logger.h
│       ├── httplib.h               # HTTP библиотека
│       └── json.hpp                # JSON библиотека (nlohmann)
├── server/                         # Серверная часть
│   ├── server_main.cpp             # Точка входа сервера
│   ├── server.cpp                  # Основная логика сервера
│   ├── server.h                    # Заголовочный файл сервера
│   ├── CMakeLists.txt              # Конфигурация сборки сервера
│   ├── server_config.json          # Конфигурация сервера
│   ├── src/                        # Модули сервера
│   │   ├── CMakeLists.txt
│   │   ├── control_plane.cpp       # Контрольная плоскость (HTTP API)
│   │   ├── control_plane.h
│   │   ├── data_plane.cpp          # Плоскость данных (UDP обработка)
│   │   ├── data_plane.h
│   │   ├── session.cpp             # Управление сессиями
│   │   └── session.h
│   ├── Data/                       # Динамические данные
│   │   ├── CDR_journal.csv         # CDR журнал
│   │   ├── IMSI_blacklist.txt      # Черный список IMSI
│   │   └── pgw.log                 # Логи сервера
│   ├── scripts/                    # Вспомогательные скрипты
│   │   ├── clear_data_files.sh     # Очистка данных
│   │   ├── generate_blacklist.sh   # Генерация черного списка
│   │   └── CMakeLists.txt
│   └── test/                       # Тесты сервера
│       ├── CMakeLists.txt
│       ├── server_main_test.cpp    # Unit-тесты
│       └── .gitignore
├── CMakeLists.txt                  # Основной файл сборки
└── .gitignore                      # Исключения Git
```

## ⚙️ Требования
- Компилятор C++ с поддержкой C++17
- CMake версии 3.10 или выше
- Linux (тестировалось на Ubuntu 20.04+)
- Git для загрузки зависимостей


## 🚀 Быстрый старт
Сборка проекта
```bash
# Клонирование репозитория
git clone <repository-url>
cd PGW_Protei
# Создание директории сборки
mkdir build && cd build
# Конфигурация проекта
cmake ..
# Сборка
make
```

Запуск сервера
```bash
# Запуск сервера с конфигурацией
./bin/pgw_server ../config/server_config.json
```
Запуск клиента
```bash
# Запуск клиента для тестирования
./bin/pgw_client ../config/client_config.json
```
## ⚙️ Конфигурация
### Конфигурация сервера (server_config.json)
```json
{
  "udp_ip": "0.0.0.0",
  "udp_port": 9000,
  "session_timeout_sec": 30,
  "cdr_file": "../Data/CDR_journal.csv",
  "http_port": 8080,
  "graceful_shutdown_rate": 10,
  "log_file": "../Data/pgw.log",
  "log_level": "INFO",
  "blacklist": "../Data/IMSI_blacklist.txt"
}
```
### Конфигурация клиента (client_config.json)
```json
{
  "server_ip": "127.0.0.1",
  "server_port": 9000,
  "log_file": "client.log",
  "log_level": "INFO"
}
```

## 🌐 HTTP API

Сервер предоставляет следующие HTTP эндпоинты:  
- Проверяет статус абонента по IMSI:
```http
GET /check_subscriber?imsi=<IMSI>
```
Ответы:
```text
    active - сессия активна
    not active - сессия не найдена
```
- Инициирует graceful shutdown сервера.
```http
POST /stop
```
Параметры:

    rate (опционально) - скорость завершения сессий (сессий/секунду)


## 📊 Форматы данных
UDP-запрос:
```text
IMSI в BCD-кодировке (согласно TS 29.274 §8.3)

UDP-ответ
    - created - сессия создана
    - rejected - запрос отклонен (черный список или ошибка)
```

CDR запись
```text
[timestamp] [IMSI] [action]

Пример:
    2024-01-15 10:30:45 001010123456789 CREATED
    2024-01-15 10:31:15 001010123456789 DELETED
```

## 📈 Логирование

Проект использует многоуровневое логирование:  
- DEBUG - отладочная информация
- INFO - информационные сообщения
- WARN - предупреждения
- ERROR - ошибки
- CRITICAL - критические ошибки

Уровень логирования настраивается в конфигурации

## 🔧 Используемые библиотеки
```text
- JSON: nlohmann/json (v3.11.2)
- HTTP сервер: cpp-httplib (v0.12.1)
- Логирование: spdlog (v1.11.0)
- Тестирование: Google Test (v1.13.0)
- CMake утилиты: FetchContent для управления зависимостями
```

## 🎯 Особенности реализации
- Модульная архитектура - каждый компонент независим
- Graceful shutdown - контролируемое завершение работы
- Потокобезопасность - защита разделяемых ресурсов
- Конфигурация без перекомпиляции - все настройки в JSON
- Полное покрытие логами - все ключевые операции логируются

